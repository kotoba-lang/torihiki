# falsify iteration — 2026-09-04 00:13–00:14 JST (cron, コード変更なし)

HEAD dd55c85。前回 remeasure-2341 (2026-09-03 23:24–38) に続く追認 iteration。

## load gate

- 00:13 (iteration start): 12.78 / 14.25 / 15.86 — 15-min < 20 → 実行可
- 00:14 (全実測後): 11.46 / 13.72 / 15.59

## hypothesis (反証対象)

H1: bench-tape-cancel-arity は HEAD dd55c85 で依然 OPEN である
    (run-tape が `cancel!` の現行 owner 必須署名に未追従のまま)。
    反証されるなら bench が赤ではなく緑で走るはず。

H2: seeded fuzz 16 seeds の digest は JVM と nbb で byte-identical であり、
    2026-09-03 23:11 baseline とも一致する (再現性の既存根拠の追認)。

## 実測

1. H2 — JVM fuzz: `clojure -M -e '(load-file "evidence/fuzz-seeded.cljk") (fuzz-seeded/run)'`
   → exit 0, evidence/fuzz-jvm-0013.out (00:13)
2. H2 — nbb fuzz: `nbb --classpath "$(nbb script/nbb-classpath.cljk)" evidence/fuzz-nbb-driver.cljk`
   → exit 0, evidence/fuzz-nbb-0013.out (00:13)
   - `grep '^seed'` の diff → **identical** (JVM==NBB)。
   - seed 14/15 の digest 行は remeasure-2341 (23:38) の出力と目視一致
     → seed 固定 digest byte-identical は **10 度目** の実測で成立。
3. H1 — bench: `clojure -M:bench`
   → exit 1, `Execution error (ArityException) at torihiki.bench/run-tape (bench.clj:112).
   Wrong number of args (2) passed to: torihiki.book/cancel!`
   (00:14, evidence/bench-0014.err) → **H1 は反証されず** (赤の存続を追認)。

## verdict

- H1: survived — bench-tape-cancel-arity は依然 OPEN。NEXT (bench.clj:112 の
  owner 必須署名への追従) は本 iteration の「コード変更なし」制約により未実施。
- H2: survived — byte-identical 10 度目。既存根拠の追認のみ。

## スコア判定

全軸 remeasure-2341 版から変化なし:

- テスト 3 / 反証 3 / 再現性 2 / spec 3 / 実装 3 / governor 1 / 運用 1
- 再現性 2→3 は bench 赤修正 + 低負荷 3 回安定実測の条件が未達のため見送り
  (修正はコード変更を伴うため本 iteration の範囲外)。
