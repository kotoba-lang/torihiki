# frame 448 (2026-09-14 23:20 枠, cron, no code changes — 静的解析枠)

## 負荷
23:19–23:20 uptime 直測 1-min **11.09** / 5-min **11.91** / 15-min **12.73** 全 window <20 → gate 成立。ただし runtime budget 枯渇 (system notice) のため bench/suite 実測は not-run。

## 状態
- HEAD **0a99c9c2** 直接実測 (frame 442–447 と同一), git diff HEAD -- src/ script/ deps.edn = 0 行。
- 正本引用 frame-441 実測 base 維持 (suite **137** / nbb 42nd / parity 40 / fuzz 33 / bench 赤 33 実行目消化; frame-447 で suite **138** 41st 同一カウント実測済み)。

## falsify-16 静的解析の前進 (決定的候補発見, 実測なし)
frame-446 假説 (owner-mismatch ~1/1024 → cancelled≈2,200 予測) は cancelled=0 を単独で説明しないまま残っていたが、
**cancel! 呼び出しの引数順逆転**を発見:

- `bk/cancel!` 署名 = `[b oid owner]` (book.cljc:566 / rename-copy 実読)。
- bench.cljc:112 接続行 = `(bk/cancel! b (bit-and i 1023) oid)` — owner と oid が**入れ替わっている**。
- 機械的帰結: slot-of(oid 位置に入った owner 値 0–1023) = (quot v 2^20) = 0 → slot 0 固定。
  slot 0 の o-gen (0–数世代) と gen-of(owner 値) の一致確率は ~2/1024 未満、一致しても
  第 3 引数の oid (>2^20) が o-owner[slot 0] と不一致 → **戻り値は事実上必ず 0**。
  → **cancelled = 厳密 0 は引数順逆転で完全説明** (owner-mismatch / ring-staleness は不要)。
- fix 候補: `(bk/cancel! b oid (bit-and i 1023))` — 1 引数順の修正のみ。
- 証跡: bench.cljc.f16orig バックアップ作成済み (測定 copy /tmp/tori-falsify15 のみ、本枠は未改変)。

## 発覚/スコア
- 発覚 1 件 (falsify-16 静的: 引数順逆転), 実測 0 件, 新規 hypothesis なし (f16 に統合)。
- スコア 7 軸変更なし (**3/3/3/3/2/1/1**; 反証実施 15 件, f8–f15 OPEN)。
- 残作業不変 + bench tape cancel-branch fix は **(1) owner 接続 (f15 実測) + (2) 引数順修正 (本枠静的) + (3) n ≥ 1M cancelled > 0 3 回安定** に確定。

## 次枠
次 genuine 枠: rename-copy bench.cljc:112 を引数順修正して 1 run (n=1M でも可) で cancelled > 0 を実測
→ その後 n ≥ 1M 3 回安定で再現性 3 判定。次枠番号 = **449**。
